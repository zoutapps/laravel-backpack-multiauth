
Route::group([
    'namespace' => '{{singularClass}}',
    'prefix' => '{{singularSlug}}'
], function () {
        Route::auth();
        Route::get('/logout', 'Auth\LoginController@logout')->name('logout');
});
